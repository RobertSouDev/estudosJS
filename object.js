// object 

const vazio = {} //obect vazio

const person = {
    name: 'John',
    age: 30,
    weight: 88.6,
    isAdmin: true
}

// para pega um nome de um object

console.log(person.name + " tem " + person.age + " anos")
